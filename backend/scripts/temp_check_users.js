const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/aaes_db').then(async () => { const User = require('./models/User'); const users = await User.find({ email: { $in: ['s1@example.com', 'admin@example.com', 'hod1@example.com', 't1@example.com'] } }); console.log(users.map(u => ({ email: u.email, isActive: u.isActive, role: u.role, requiresPasswordChange: u.requiresPasswordChange }))); process.exit(0); })
